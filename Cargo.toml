[workspace]
members = [
    "common",
    "antd_service",
    "rust_admin",
    "rust_stream",
    "standalone_launcher",
]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
